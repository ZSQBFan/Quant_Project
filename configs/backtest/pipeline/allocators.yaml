# 权重分配器配置
#
# 定义所有可用的权重分配器及其参数

# ============================================================================
# EqualWeight 等权重分配器
# ============================================================================
# 所有选中股票等权重配置

EqualWeight:
  description: "所有选中股票等权重分配"
  params: {}
  enabled: true

# # ============================================================================
# # FactorWeight 因子权重分配器（预留）
# # ============================================================================
# # 根据因子值大小按比例分配权重

# FactorWeight:
#   description: "根据因子值按比例分配权重"
#   params:
#     normalize: true  # 是否归一化
#   enabled: false

# # ============================================================================
# # RiskParity 风险平价分配器（预留）
# # ============================================================================
# # 使用风险平价方法分配权重

# RiskParity:
#   description: "风险平价权重分配"
#   params:
#     lookback_period: 60  # 计算波动率的回看周期
#   enabled: false

# # ============================================================================
# # MaxSharpe 最大夏普比分配器（预留）
# # ============================================================================
# # 优化权重以最大化组合夏普比

# MaxSharpe:
#   description: "最大化夏普比的权重分配"
#   params:
#     lookback_period: 120
#     risk_free_rate: 0.02
#   enabled: false
